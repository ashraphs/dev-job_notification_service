[x] 1. create initial loader - run only for the first time
        - [x] query the job (example: card update job) *limit 1 result
        - [x] insert the result to table= jn_abt_job
        - [x] same to other job

 [] 2. Query the job based on job name
        - [] scheduling job = every 3 hour
        - [] import the latest job to the table= jn_abt_job
        - [] same to other job

  [] 3. Create the template for job status

  [] 4. Send job notification using webhook -dingtalk
         - [] save status to the table=jn_notification_message

